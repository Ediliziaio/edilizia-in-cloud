

# Piano: Ingrandire i mini-stats Fatturato/Incassato/Costi Pagati

Aumentare le dimensioni del testo da `text-[11px]` a `text-sm` per le label e `text-base font-bold` per i valori, e aumentare il separatore da `h-3` a `h-4`.

**File:** `src/components/cruscotto/TodayFocus.tsx` (righe 140-155)

Cambiare:
- Container: `text-[11px]` → `text-sm`, `gap-3` → `gap-4`
- Valori: `font-semibold` → `text-base font-bold`
- Separatori: `h-3` → `h-4`

