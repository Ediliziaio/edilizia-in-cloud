

# Footer: Sfondo Bianco e Testi Blu

## Modifica

### File: `src/components/landing/LandingFooter.tsx`

Cambiare lo schema colori del footer:

- **Sfondo**: da `bg-[#0f1d35]` (blu scuro) a `bg-white` (bianco)
- **Bordo top**: da `border-white/[0.06]` a `border-gray-200`
- **Testi principali** (titoli h4): da `text-white` a `text-[#1a2744]` (blu navy del brand)
- **Testi secondari** (paragrafi, link, dati): da `text-white/40` a `text-[#1a2744]/60`
- **Logo**: rimuovere `opacity-80` per visibilita su sfondo chiaro
- **Hover link**: mantenere `hover:text-[#0fa68c]` (verde teal del brand)
- **Separatore barra inferiore**: da `border-white/[0.06]` a `border-gray-200`
- **Copyright e link legali**: da `text-white/30` a `text-[#1a2744]/40`

Nessun cambio strutturale, solo colori.

