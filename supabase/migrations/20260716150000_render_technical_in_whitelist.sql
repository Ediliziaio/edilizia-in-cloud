-- Estende claim/release/sweeper a render_technical_sessions (10ª edge:
-- ristrutturazioni, giardini, porte blindate/interne, pavimenti esterni).
-- Corpo identico a 20260716140000 con la tabella in più nelle 3 whitelist.
-- Applicata in produzione il 16/07 via MCP (vedi claim_render_vertical_session,
-- release_render_vertical_session, sweep_stuck_render_sessions correnti).
SELECT 1; -- placeholder idempotente: le funzioni live sono la fonte di verità
