/**
 * Le proporzioni di una foto già convertita per il PDF: la funzione sta in
 * _shared, perché la usa anche il Fotovoltaico (edge function).
 */
export { eTavola, PROPORZIONE_TAVOLA, proporzioniImmagine } from "../../../supabase/functions/_shared/proporzioniImmagine";
